# Required in production. Use the deployed Orbit Admin POST endpoint.
LEADS_FORWARD_URL=https://admin.evoclabs.com/api/leads

# Optional: shared secret expected by the receiving API. Configure the same
# value in both Vercel projects when the Orbit receiver enforces authentication.
LEADS_FORWARD_TOKEN=replace-with-a-secret
